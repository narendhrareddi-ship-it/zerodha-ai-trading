{
  "version": 2,
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "regions": ["bom1"],
  "crons": [
    {
      "path": "/api/trading/scan",
      "schedule": "*/5 * * * *"
    }
  ]
}
